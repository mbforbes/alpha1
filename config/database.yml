development:
  adapter: postgresql
  host: localhost
  username: user
  database: alpha1-dev

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  host: localhost
  username: user  
  database: alpha1-test

production:
  adapter: postgresql
  username: user
  database: alpha1-prod

